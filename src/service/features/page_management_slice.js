import { createSlice } from "@reduxjs/toolkit";

const name = "pageManagement";
const initialState = {

}

const pageManagementSlice = createSlice({
    name,
    initialState,
})

export default pageManagementSlice;